buildscript {
  repositories {
    maven {
      url "https://plugins.gradle.org/m2/"
    }
  }
  dependencies {
    classpath "org.sonarsource.scanner.gradle:sonar-gradle-plugin:4.8.0.2856"
  }
}

apply plugin: "org.sonarqube"

sonar {
  properties {
    property "sonar.projectKey", "sample_check_freestyle"
  }
}
